import eslintPluginAstro from 'eslint-plugin-astro';
import tseslint from 'typescript-eslint';

export default tseslint.config(
  {
    // env.d.ts is Astro-generated boilerplate (regenerated by `astro sync`/`astro dev`).
    ignores: ['dist/**', '.astro/**', 'node_modules/**', 'src/data/kb.json', 'src/env.d.ts'],
  },
  ...tseslint.configs.recommended,
  ...eslintPluginAstro.configs['flat/recommended'],
  {
    rules: {
      // Astro/Preact components intentionally leave some props/args unused
      // (e.g. destructured Astro.props); warn instead of erroring.
      '@typescript-eslint/no-unused-vars': ['warn', { argsIgnorePattern: '^_' }],
    },
  },
);
