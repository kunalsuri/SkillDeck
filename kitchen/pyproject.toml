[project]
name = "kitchen"
version = "0.1.0"
description = "Offline processing pipeline for SkillDeck"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.34.2",
    "pyyaml>=6.0.3",
    "datasketch>=2.0.0",
    "jsonschema>=4.26.0",
]
